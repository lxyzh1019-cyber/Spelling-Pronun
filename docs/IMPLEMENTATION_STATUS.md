# Implementation Status

Updated: 2026-09-09 (America/Edmonton), after the Codex audit of `6f76557`
Baseline: `58fe21ebb6ad351094d4686a9713e6ccbdba6744`
Working release: R2 Pilot Engine

This ledger tracks implementation evidence separately from content review, real-device testing, and family piloting. A checked automated test does not imply educational or iPad validation.

## Requirements and repairs

| ID | Owner | Release | State | Artifact/commit | Verification evidence | Blocker/next action |
|---|---|---|---|---|---|---|
| GOV-01 | Codex + parent | R1–R3 | integrated | `docs/MASTER_PLAN.md`, this ledger | Master plan and one status ledger present | Maintain per release |
| F01 / ENG-01 | Codex | R1 | verified | `src/context/WordProvider.jsx` | Baseline failure reproduced; `npm run build` passes after repair; GitHub Actions passed on the working PR | Monitor checks after each push |
| F02 | Codex | R1 | verified | `src/pages/SpellingTest.jsx` | Unit test plus browser completion → Try Again → word 1, zero score | PR check |
| F03 | Codex | R1 | verified | spelling score helpers | Unit test; 25-skip browser completion showed no perfect result | PR check |
| F04 | Codex | R1 | verified | daily session route/evidence | Unit test; browser used exact five-word route and retained completion after five skips | Remote authenticated path still needs device test |
| F05 | Codex | R1 | verified | learner-bound multiplayer child sessions | Browser switch Jenn → Jess → Jenn showed isolated start/resume state | Verify remote event attribution after Firebase auth setup |
| F06 | Codex | R1 | verified | dynamic crossword generator | Long-word invariant test; browser rendered an 11×11 valid puzzle | Real iPad layout check |
| F07 | Codex → Claude | R1 | verified | batched attempt events and functional progress; word-game attempts queue to the IndexedDB outbox when a cloud batch fails | Five-result unit test; crossword browser check/repair path; outbox delivery test proves a failed entry and everything after it stay queued and a retry writes each exactly once; word-attempt flush rewrites word totals from local counts with a merge instead of `increment` | Remote reconciliation still needs an authenticated Firebase run |
| F08 | Codex | R1 | integrated | evidence labels across existing games | Unit test for `self_report`; source inspection for assisted/timed/game labels | R2 mastery engine must enforce eligibility |
| F09 | Codex | R1 | verified | auth timeout, local store, truthful status | Browser showed local-only status after auth configuration failure; reload retained session | Verify online state after Firebase setup |
| F10 / DATA-02 | Parent + Codex → Claude | R2 | integrated locally / externally blocked | Parent sign-in/register UI; local-to-cloud attempt reconciliation; explicit import preview with per-learner counts for an existing account (Import / Skip for now), recorded import decisions (`spelling-users.imports`), create-path additive import, and local-only word rows preserved instead of overwritten | Automated sync invariants pass; import-preview unit tests prove exact new/already-in-cloud counts, a repeated import proposes nothing, and automatic push is refused for a non-empty account until the parent decides; production-browser render of `/parent` in local-only mode shows no runtime errors (the signed-in preview branch needs Email/Password auth and is source-verified only); browser verified sign-in/create mode, local-only fallback, and ordinary Jenn/Jess state isolation; source/build review verifies late save/sync callbacks write only their captured learner key and cannot mutate the newly active learner | Parent must enable Email/Password in Firebase; then run two-device and in-flight-save reconciliation tests |
| F11 | Codex | R1 | verified | learner/category-bound session snapshot | Unit test rejects wrong learner; browser reload resumed word 2 with original skip count; R2 local mirrors now carry and validate mirror schema, session ID, learner, mode, content version, and ordered item IDs before fast bootstrap | IndexedDB/outbox expansion is delivered under DATA-01; remote device verification remains under DATA-04 |
| F12 | Codex | R1 | integrated | game hints remain game-scoped; local hints work | Source review and production build | Full lesson help ladder arrives in R2 |
| F13 | Codex | R2 | verified | production-only service-worker registration and assessment lifecycle notice | Production browser testing reproduced a stale development app shell because the service worker cached `/src` modules; registration is now guarded by `import.meta.env.PROD`; a regression test enforces the guard; the rebuilt production preview loads lesson, story, and assessment routes with truthful integrated/partial-integration notices and no application runtime errors | Keep real-iPad offline/update testing in the device gate |
| DATA-01 | Codex → Claude | R2 | verified locally (reconnect flush added 2026-09-08) | evaluator, mastery, scheduling, version-pinned delayed-review runner, revision-guarded IndexedDB session snapshots, identity-bound local mirrors, attempt outbox, and learner-scoped local bootstrap | Recovery tests prove corrupt/missing local state restores only a matching learner/mode/content version; isolated production-browser harnesses verified missing and malformed mirrors restore question 3 at a monotonic revision, and separately proved unversioned, wrong-learner, and wrong-content-version mirrors are rejected, restored from compatible IndexedDB state, and rebuilt with explicit session/learner/mode/version metadata; unit coverage also rejects unknown mirror schemas and reordered item IDs; older IndexedDB revisions cannot replace newer snapshots; lesson-completion records are content-version pinned; browser verified exact lesson and assessment resume after route interruption and confirmed review bootstrap; draft content remains excluded from mastery. 2026-09-08: scheduler now treats any same-Edmonton-date retry as non-advancing (helped or not); mastery derivation v2 flags `needsReview` after two failures in the last five eligible attempts and caps status at developing without erasing history; evidence eligibility rejects any attempt with ordinal > 1 for the same session/item; sentence-repair evaluator adds scoped structural checks (declared clauses joined only by period, semicolon, or a listed coordinating conjunction) and typed C0 sentence items permit `Review this answer` (pending) for unlisted wording; unit tests cover each rule and its rejection path | Connect authenticated remote sync after Firebase setup; exercise the review runner after C0 is independently reviewed |
| DATA-03 | Codex | R2 | verified locally | expiring per-tab leases and explicit takeover for resumable lesson, assessment, and review sessions | Three deterministic lease tests prove second-writer refusal, explicit takeover, stale-writer rejection, and expiry recovery; a two-tab production-browser check showed tab 2 open read-only with its action disabled, explicit takeover enabled tab 2, and the storage event immediately made tab 1 read-only and disabled its action; all session inputs and late cursor writes are ownership-guarded | This local same-browser lease is not cross-device arbitration; verify the separate authenticated cloud-owner contract on two linked devices after Firebase setup |
| DATA-04 | Codex + parent | R2 | integrated locally / externally blocked | Authenticated durable-session hook, stable device identity, serialized Firebase transaction adapter, and owner-scoped `spelling-sessions` rules | Pure contract tests prove first claim, returning-owner sync, second-device read-only, state-preserving explicit takeover, owner epochs, monotonic revisions, stale-owner/revision rejection, path-safe IDs, and pinned learner/mode/content/order; browser verified auth failure still permits saved local repair work without runtime errors | Deploy the rules after parent authorization, enable Email/Password auth, then verify claims, queued saves, takeover, and stale-write rejection with two linked clients; no live Firebase session write has occurred |
| EDU-01 | Codex → Claude | R2 | 96 lesson objects and 2 story episodes integrated; not learner-tested or released | `/case`, `lessonCatalog.js`, `lessonFlow.js`, `/lesson/:sessionId`, review records, `integration.c0.json` | Browser completed the C0 spelling and capitals lessons; two misses produced a worked solution; `I don’t know yet` remained unresolved and entered guided repair; pause/back resumed that exact repair; all 96 lesson objects and both story episodes completed challenge, separate educational/source review, and explicit integration validation; reviewed/integrated content remains excluded from release/mastery. 2026-09-08 lesson-loop additions (§6): per-learner lesson length guidance (10/15/20 min, default 20, stored locally, no clock in any transition — enforced by test), lesson-level `Audio or microphone did not work` deferral that is neither a miss nor a retry, one-rung-at-a-time help ladder with `More help`, optional reflection note alongside the required choice, a resume recap captured only when a saved session is restored, and optional `ruleHelpZh` Chinese rule help validated when present and rendered behind a `中文提示` toggle (no C0 translations authored; content is GPT-owned). Production-browser check: length selector persists across reload, help ladder reveals steps in order, a correct first answer then reload shows `finished 1 of 8 tasks` and question 2 | Learner-test the lesson packs and story on the target iPad before release |
| EDU-02 | Codex | R2 | 68/68 challenged; 28/68 reviewed and integrated; not released | Full 34-item `/assessment/:sessionId` Form A/B runner, `/progress`, `/review`, `/parent`, assessment review records, `integration.c0.json`, reviewed-audio registry/playback seam | Browser showed 1/34 → reload at 2/34 and transition from Part A to Part B at 21/34; pause/resume, duplicate-submit protection, and learner isolation were exercised; every prompt has an item-level challenge result; all 28 Part-B prompts have explicit educational/source-review and integration coverage; all 40 Part-A prompts remain challenge-only with exact audio or specialist-review blockers; nineteen assessment discrepancies were corrected and recorded. Once supplied, versioned reviewed recordings are selected over synthetic preview playback, announce their locale truthfully, and cancel on item/learner changes. | Replace synthetic audio with independently reviewed human audio and complete specialist phonics/pronunciation review before placement use. 2026-09-08 (§7): completed previews are kept in a per-learner, per-form history (last six, deduplicated by completion time) and `/assessment` shows the like-for-like comparison of the latest two with exposure disclosure; the 4–6 week reassessment suggestion is shown on `/assessment` and the completed report as text only (no reminder); the real 68-prompt forms and the lesson pool now pass `validateContent` in the test suite (zero errors, no overlap). Production-browser check of `/assessment` with seeded history rendered the comparison table (`spelling 1/3 → 3/3, like-for-like yes`) with no runtime errors |
| EDU-03 | Codex → Claude | R2 | integrated draft preview | `src/components/RecordingAnswer.jsx`, `src/utils/recording.js`, `src/utils/speech.js`, `src/hooks/useCancellableSpeech.js`, IndexedDB `recordings` store | Capability/MIME/cancellation and empty/short/silent-capture tests; recording input is invalidated before Record again; Start/Stop recording controls now use a 1.125rem font, 56px height, 190px minimum width, distinct start/stop states, and visible focus treatment. Every learner-facing speech screen uses abortable playback scoped to its learner/session/item, delayed spelling timers are cleared on session change, and flashcards visibly offer retry/defer wording if playback fails while truthfully reporting the available locale. Production browser verified assessment audio status clears on item advance and Jenn/Jess switch, spelling returns to the incoming learner’s untouched session, flashcard playback cancels on Next, and a speaking technical failure advances without a wrong mark; blobs remain pending human review; 2026-09-08: a `Delete this recording` control removes the local blob, and the copy states recordings stay on the device only until deleted with nothing uploaded | Calibrate silence threshold and run microphone, playback, persistence, and human-review checks on a real iPad |
| STORY-01 | Codex | R2–R3 | chapter 1 integrated; not learner-tested or released | `story.c0.draft.json` v2, `/case`, story challenge/educational review records, `integration.c0.json` | Both sourced, visibly fictionalized episodes retain problem, language clue, decision, consequence, reveal, history note, and unresolved question. Each now has a validated two-sentence recap and replayable narration with explicit Canadian-English/fallback status; production browser verified the visible recap, playback status, and playback-state reset on Jenn → Jess switch | Learner-test chapter 1 on the target iPad; R3 still requires chapters 2–6 and ten additional reviewed episodes |

## Release gates

| Gate | State | Evidence / next action |
|---|---|---|
| R1-G1 build | verified locally | Production build remains green after R2 integration |
| R1-G2 regressions | verified locally | `npm test`: 154/154 on 2026-09-08, after the audit corrections; browser restart, skips, daily, crossword, learner ownership, reload checked. Current on 2026-09-10: 224/224 |
| R1-G3 legacy preservation | integrated | Legacy word IDs/collections remain readable; new attempt collection is additive; no migration/deletion; local-only word rows are no longer overwritten by the cloud map; an existing parent account never merges device history without an explicit import decision |
| R1-G4 auth fallback | verified locally | Firebase `auth/configuration-not-found` led to usable local-only mode without hanging |
| R1-G5 candidate report | assembled | `docs/R1_CANDIDATE_REPORT.md` lists every repair with its evidence type, separates verified from device-untested behaviour, carries F10 into R2, and records that publication remains a parent decision |
| R2 engine build | verified locally | `npm run build`: 129 modules transformed on 2026-09-08; lazy learner routes emitted. Current on 2026-09-10: 153 modules transformed |
| R2 preview journey | verified locally | Browser exercised omission → guided repair, explicit pause/exact resume, a two-miss worked solution, two complete eight-task lessons, reflection/evidence, story reveal/unlock, assessment resume, and 42-skill progress; the completed assessment now selects a matching integrated C0 lesson for spelling, grammar, sentences, or punctuation and refuses a false recommendation for uncovered tracks. The home screen now makes the four C0 spelling/sentence/punctuation/grammar lessons primary actions, puts Learn/Assessment/Progress in the header, and labels legacy word-game counts as optional activity; a built-browser check opened the grammar lesson route. Rebuilt production preview confirms truthful integrated lesson/story and partial-integration assessment notices; isolated production-browser runs verified IndexedDB recovery and local-mirror repair for missing, malformed, unversioned, wrong-learner, and wrong-content-version localStorage state; story recap narration rendered, played, and reset on learner switch |
| R2 pilot approval | granted for the audio-free C0 content | Parent decision recorded 2026-09-09 in `src/data/pilotApproval.c0.json`: the four packs (96 objects), both episodes, and the 28 Part B prompts. All 40 Part A prompts are excluded pending the listening check and, for the speaking prompts, a rater that does not exist. The validator refuses pilot approval for any item that is unreviewed, unintegrated, under correction, or dependent on unchecked audio. Pilot answers are derived into a separate evidence record and are never released evidence. The pilot review queue now serves delayed review while the released queue stays empty, which is the circular dependency the audit named |
| C0 content corrections | six resolved and installed | `corr.c0.001`–`corr.c0.006` were resolved by the parent on 2026-09-09 and their replacements installed as version 2 of each affected item. Nothing is withheld: both forms serve 34 of 34 and story episode one is whole again. Resolving a record alone never restores the defective version; the replacement must be present at the recorded version or the item stays withheld and the validator fails. The challenge records for the eight changed items were re-checked at version 2 |
| R2 C0 inventory | lesson/story integrated; assessment partially integrated | Exact counts exist: 68 assessment prompts, four 24-object packs, and two story episodes. All 166 C0 objects/episodes completed recorded full-coverage challenge passes; all 96 lesson objects, both story episodes, and 28 Part-B assessment prompts also completed separate educational/source review and integration validation. All remain blocked from release/mastery pending Part-A audio/specialist review and learner testing |
| Content release validator | verified | Manifest separately counts challenged, reviewed, integrated, and released inventory and proves C0 96/68/2 counts without claiming release readiness; validator rejects unknown tasks/sources/skills, broken audio, duplicate or malformed audio assets, an unlabelled non-Canadian locale, mismatched reviewed-audio transcripts, reviewed/released synthetic audio, unsafe recording evaluators, malformed role distributions, integration without review, and release without completed review/integration. `AUDIO_REVIEW_HANDOFF.md` lists the parent listening check and the educational review that replaced the earlier human-recording requirement. |
| R2 assessment evidence | verified preview | Unit tests independently preserve correct, wrong, skipped, helped, pending-review, and technical-failure categories, and enforce like-for-like comparison; browser showed assisted/omitted counts, a punctuation follow-up signal, and prior Form B exposure disclosure |
| R2 full assessment runner | verified partially integrated preview | Both 34-item forms run end to end and are fully challenged; 28 Part-B prompts are reviewed and integrated while 40 Part-A prompts retain exact blockers; browser verified synthetic audio control and cancellation across item/learner transitions, explicit pause/exact resume, rapid-submit protection, learner-isolated switching, speaking technical-failure continuation, exact route-interruption resume, and 20-item Part A → 14-item Part B transition; the runner now presents a matching C0 lesson only for covered tracks; the 132-test suite and production build passed on 2026-09-08; the suite is 224 tests on 2026-09-10 and still passes |
| R2 delayed-review runner | integrated / content blocked | Pure tests verify reviewed-and-released-only admission, four-item cap, stable item/version pinning, feedback preservation, machine-evaluable routing, and completion; integrated but unreleased content is explicitly excluded, as is challenge-only content; browser confirms C0 preview content cannot enter the mastery queue and its durable session bootstrap is error-free; 132 tests and production build pass | Complete learner testing, then browser-test correct, helped, omitted, revealed-repair, pause/resume, and schedule advancement with explicitly released items |
| R3 source/episode preparation | verified pre-pilot preparation only | `curriculum.c1-c2.preparation.json` maps all 38 future packs (912 future objects) to named workshops and the planned ten C1/C2 episodes. It records source-preparation status—including explicit specialist/audio gaps—and validates exact C1/C2 counts, without marking a pack authored, reviewed, integrated, learner-tested, or released. | C1/C2 final authoring remains dependent on the R2 pilot outcome; specialist phonics/pronunciation sources and item-level sources remain open where identified. |
| R3 story research preparation | verified pre-authoring preparation only | Chapters 2–6 have candidate official research destinations and an explicit claim boundary for each. The validator rejects a missing chapter or any premature `verified` status; Chapter 3 specifically remains open until relevant Indigenous nation/community sources are selected. | Select compatible period, locale, and two episode-specific sources per chapter before any learner-facing story is authored. |
| R2 external-gate tracker | verified locally | The Parent view presents the remaining C0 audio listening check, Firebase/two-device setup, real-iPad verification, pilot-approval decision, family-pilot, and C1/C2 dependencies as explicit non-ready states. A built production-browser check confirmed all five statuses and their exact dependency text. This is a readiness aid only; it cannot turn a gate green or release content. | Complete the listed external verification steps with evidence. |
| In-app human checks | verified locally | `/checks` is in two labelled halves. The **Technical Test Lab** carries 60 rows across six checks — 16 dictation words, 16 contrast utterances, 12 decoding candidates, a real-iPad pass, a practice run for resume/offline/double-submit, and the two-device check. Its audio plays in place, derived from the version-pinned assessment items; its scenarios run against invented practice questions in a `spelling-testlab-` namespace; no row in it links to a learner route. **Family Pilot Observation** carries 10 rows across two checks and refuses to open anything until the parent names the observed child and acknowledges that answers will be saved to them. A production-browser run walked from Home to `/checks`, played the first, middle and last sample of all three audio groups, played both sides of a contrast pair, recorded a problem with a note, ran the practice run offline and online, confirmed the two-device card reported its real preflight result (signed out, on this machine), confirmed the pilot link appeared only after choosing a learner and confirming, reloaded and found both the result and the practice run intact, and finished with no uncaught error. Ordinary storage was snapshotted before and after that full pass: the only key that changed was `spelling-family-device-id`, created on first read by the shared `getOrCreateDeviceId`. No attempt, progress, achievement, hint, daily-challenge, session or outbox key changed. On 2026-09-10 the same measurement was extended to IndexedDB. All five stores (`sessions`, `attempts`, `outbox`, `meta`, `recordings`) were seeded with a learner-owned row, then a full Test Lab pass ran — audio playback, all four practice questions including one answered offline, a queue flush, the two-device preflight, and `Reset this test run`. Every store came back byte-identical, no ordinary `localStorage` key changed, and the only Test Lab key present was `spelling-testlab-run:practice`, which reset recreates as an empty run. No uncaught error. Worth stating plainly: `/checks` never opens the learning database at all — the seed had to create the stores itself, because the app creates them lazily and nothing on this page does. Eleven mutations were checked, each confirmed to fail a test | This is a way to collect the external evidence, not the evidence. Every gate below still needs the real event |
| iPad/PWA install metadata | verified locally | Missing icon repaired; honest manifest uses real 192/512 PNGs, 180px Apple touch icon, standalone metadata, and no fake screenshot entries; automated asset checks pass | Real Safari install/offline/microphone test still required |

## Content manifest

| Batch | Required | Authored | Independently challenged | Educational/source reviewed | Integrated | Unresolved discrepancies |
|---|---:|---:|---:|---:|---:|---:|
| C0 assessment A/B | 68 prompts | 68 draft | 68 | 28 | 28 | 0 |
| C0 four pilot packs | 96 objects | 96 draft | 96 | 96 | 96 | 0 |
| C0 chapter 1 | 2 episodes | 2 draft | 2 | 2 | 2 | 0 |
| C1 | 480 objects + 6 episodes | 0 | 0 | 0 | 0 | 0 |
| C2 | 432 objects + 4 episodes | 0 | 0 | 0 | 0 | 0 |

## Defects

| ID | Severity | Requirement | Found | Disposition | Commit |
|---|---|---|---|---|---|
| DEF-01 | High | DATA-01 §8 | `reviewScheduler.js` only blocked *helped* same-day retries from advancing and carried a dead ternary; an unhelped same-day retry advanced the schedule | Fixed: any same-Edmonton-date retry is non-advancing; unit-tested | batch 1 |
| DEF-02 | High | DATA-01 §8 | Mastery regression rule (two failures in last five) only applied to `secure`; developing skills never re-entered review | Fixed: `needsReview` derived for every status, status capped at developing, history preserved; derivation version 2 | batch 1 |
| DEF-03 | Medium | DATA-01 §8 | "First attempts" not enforced in `evidenceEligible`; a page bypassing `lessonFlow` could count a retry | Fixed: attempts carry `ordinal`; ordinal > 1 is ineligible; technical failures do not consume the first ordinal | batch 1 |
| DEF-04 | Medium | EDU-01 §8 | `Review this answer` path unreachable: no C0 item set `allowReview`; sentence repair had no scoped structural acceptance and no semicolon handling | Fixed: typed C0 sentence items allow review; `repairScope` structural evaluator added with rejection tests; `/progress` shows pending count | batch 1 |
| DEF-05 | High | DATA-01 §9 / §13 "offline, reconnect" | The attempt outbox flushed only on mount or after a submit; reconnecting never triggered delivery, and word-game batches that failed were dropped with no retry | Fixed: `online`/`visibilitychange` flush; word-game attempts queued as `word-attempt`; delivery planned as idempotent writes; unit-tested | batch 2 |
| DEF-06 | Medium | F08 / §11 achievements | Stat-based badges lived only in React state (lost on reload); manual badges were dropped in local-only mode; concurrent awards overwrote each other (last write wins); `daily_champion` was defined but never awarded | Fixed: awards persisted locally and merged by ID with the cloud record; cloud writes use `arrayUnion`; `daily_champion` awarded when the five-word challenge is first completed; unit-tested | batch 2 |
| DEF-07 | Low | §9 DST tests required | Only one Edmonton midnight case was tested; no DST transition or device-zone independence test | Fixed: spring-forward, fall-back, and `TZ=Asia/Shanghai` cases added. Note: a first version asserted the November 2026 fall-back and failed on the CI runner (Node 24) while passing on Node 22; tzdata for future transitions differs between Node releases, so the test now uses the settled 2024 transitions. Browser day keys use the device ICU; no app change was needed | batch 2 / 7 |
| DEF-08 | High | F10 §9 / §13 "legacy import repeated twice" | Signing into an existing parent account merged local history immediately with no preview or counts; word rows that existed only on the device were replaced by the cloud map and dropped from local storage on the next write | Fixed: explicit preview with counts and Import/Skip; decisions recorded locally and in `spelling-users`; automatic push gated by `canAutoPush`; local-only word rows retained and flagged | batch 3 |
| DEF-09 | Medium | EDU-01 §6 | Lessons had no technical-failure path (only assessments did), so an audio problem in a lesson could only be recorded as an omission | Fixed: lesson deferral path added; flow test proves no retry count and no repair | batch 4 |
| DEF-10 | Low | EDU-02 §7 | `compareAssessmentReports` was tested but unreachable in the app; no reassessment interval guidance; validator overlap rule never ran on the real forms | Fixed: per-form completion history + comparison view; suggestion text; real-form validation test | batch 5 |
| DEF-11 | Medium | §2 additional checks (crossword) | Crossword `Check` revealed every solution letter before the repair phase, so repair was copy-back rather than retrieval, and repair typing produced no evidence | Fixed: phases solving → checked → repair → revealed; solution letters render only after `Show Answers` (available after one repair round); cells of correct entries stay locked; repair results are recorded separately as `assisted_repair`; original results recorded once. Browser-verified in the production build | batch 6 |
| DEF-12 | Medium | §2 additional checks (Speed Round) | Untimed 200-word option was labelled `Short Round`, tagged `timed_retrieval_practice`, repeated words when the pool was small, had no double-submit guard, and discarded misses | Fixed: `Practice (200 words, untimed)` tagged `untimed_practice`; list capped at pool size; one submission per word; misses and passes listed for review on the finish screen; unit + browser checks | batch 6 |
| DEF-13 | Low | §2 additional checks (Best Streak) | `Best Same-Word Streak` was the maximum of *current* per-word streaks and decreased after a miss, so streak badges could show a lower number than their threshold | Fixed: per-word `bestStreak` historical maximum stored locally and in cloud rows; stat never decreases; unit-tested | batch 6 |
| DEF-14 | Medium | SPEECH-01 §10 | Recordings persisted in IndexedDB with no delete control while the copy implied session-only storage | Fixed: `Delete this recording` control and `deleteRecording`; copy states recordings stay on this device only until deleted and nothing is uploaded | batch 6 |

### Defects found by the Codex audit of `6f76557` (2026-09-08)

| ID | Severity | Requirement | Found | Disposition | Commit |
|---|---|---|---|---|---|
| DEF-15 | High | EDU-01 §5 content correctness | The punctuation pack explained "Falling rocks." as a complete statement; it is a fragment used conventionally on a sign, and SE.complete is separately assessed | Fixed 2026-09-09: parent approved the corrected explanation, installed as version 2. It names the sign wording a fragment and contrasts sign style with ordinary writing. Story episode one's transfer task is restored | batch 2026-09-09 |
| DEF-16 | High | EDU-01 §5 content correctness | The spelling pack described *watch* as using "the short a sound", which does not match ordinary Canadian pronunciation | Fixed 2026-09-09: parent supplied and approved the wording, installed verbatim as version 2. It states the tch pattern and compares the vowel to wash without naming a phonics category | batch 2026-09-09 |
| DEF-17 | High | EDU-02 §7 | The four invented-word decoding prompts supplied the pronunciation, so they measured matching a given pronunciation to a segmentation rather than independent decoding | Fixed 2026-09-09: parent replaced the proposed print-only redesign with a machine-scored receptive task. The learner sees the printed word and picks which of three recordings matches it, reported as recognizing a plausible pronunciation from spelling. Reading aloud is optional practice, never scored. The track carries a disclosure that independent reading aloud is not measured. The twelve recordings still need the listening check, and the validator blocks pilot approval until then | batch 2026-09-09 |
| DEF-18 | High | EDU-02 §7 | Both editing prompts asked for exactly four corrections without an explicit intended meaning; Form A's repair was ambiguous between a present reading (`are` correct) and a past reading (`were`) | Fixed 2026-09-09: both prompts state the intended meaning, and the rubric carries a model answer, exactly four named targets, and the alternative that is not accepted | batch 2026-09-09 |
| DEF-19 | High | DATA-01 §8 | `/progress` counted an attempt as eligible whenever its content status was not `not_reviewed`, so pilot-preview attempts displayed as eligible while mastery excluded them | Fixed: the screen counts only released, eligible attempts and reports pilot and not-counted attempts separately | batch A |
| DEF-20 | High | EDU-02 §7 | Every run of a form submitted under the same session ID, so a retake was recorded as further attempts in the original session and lost first-attempt eligibility | Fixed: each run mints an attempt session ID; resuming keeps it, clearing mints a new one | batch A |
| DEF-21 | High | DATA-02 §9 | The outbox wrote a device's local word totals into the shared progress row, so a device with three attempts could overwrite a cloud total of ten | Fixed: totals are derived from the immutable attempts, legacy imports are kept as a recorded base, and a derived total lower than the stored one is held back rather than written | batch B |
| DEF-22 | High | DATA-01 §8 evaluators | The sentence-repair evaluator accepted `I packed my bag, and i forgot my goggles.` as correct | Fixed: a coordinating join no longer lower-cases the pronoun I or a declared proper noun; ordinary clauses still lower-case | batch A |
| DEF-23 | High | DATA-01 §8 evaluators | The sentence-repair evaluator required a capital after a semicolon, so `The bell rang; everyone entered.` was rejected and `; Everyone` accepted. A semicolon joins one sentence and does not start a new one | Fixed: only a period requires a capital; a word that keeps its capital anywhere still keeps it | batch 2026-09-09 |
| DEF-24 | Medium | DATA-02 §9 | Word-total reconciliation read the row, derived, then wrote. A total another device wrote in between was overwritten | Fixed: each row is read and rewritten inside its own transaction; the derivation is pure, so a retried transaction produces the same document | batch 2026-09-09 |
| DEF-25 | High | F10 §9 | A sync flushed the whole outbox, so one learner's approved import pushed another learner's queued answers even when that learner had declined | Fixed: a sync sends only the learner it is syncing; other entries stay queued and untouched | batch 2026-09-09 |
| DEF-26 | Medium | DATA-02 §9 | An imported legacy total already included answers still queued in the outbox, so those answers were counted again when they sent | Fixed: the imported row records which attempt IDs it already covers and the derivation skips them | batch 2026-09-09 |
| DEF-27 | Medium | EDU-02 §7 | The assessment self-check lived in component state, so a reload skipped it and it was not tied to the learner's saved session | Fixed: the self-check and the new optional-practice step live in the saved session, which is keyed by learner | batch 2026-09-09 |
| DEF-28 | High | QA-01 §14 | Thirteen tests read a component as text and asserted on its wording, so they passed whether or not the component worked. Two audit findings, the progress-screen miscount and the reused assessment session id, lived in exactly that blind spot. Three files had real rules and no test of any kind | Fixed: the rules moved into pure modules and are executed by tests. Every remaining source-text test is renamed as a source guard or copy guard, with a header saying it does not execute the component. Five mutations were checked to confirm the new tests fail when the rule is broken | batch 2026-09-09 (testing) |
| DEF-29 | Medium | QA-01 §14 | Eleven extensionless relative imports meant the IndexedDB module and the hooks could not be imported under plain node, so the storage layer could not be tested at all without a bundler | Fixed: extensions added; the storage functions now run in a test | batch 2026-09-09 (testing) |
| DEF-30 | Critical | QA-01 §14 | Extracting `withIds` and `slug` out of `WordProvider.jsx` removed their definitions without adding the import, so the app threw `withIds is not defined` on load and rendered nothing at all. `npm run build` passed, because a bundler treats an unknown name as a global, and 192 tests passed, because none of them render a provider. It was found only by opening the production preview in a real browser | Fixed: the import added, and the app confirmed rendering again in the production preview. A source guard, `test/componentImports.test.js`, now reads every `.jsx` file and fails when it calls a helper exported by `src/learning`, `src/persistence` or `src/utils` without importing it; removing the import again was confirmed to fail it | batch 2026-09-09 (checks) |
| DEF-31 | Critical | QA-01 §14 | `LearningProvider` derived mastery with `attempt.skillIds.includes(...)`, but an attempt need not name a skill: `createAttempt` writes word-game attempts with no `skillIds`, and anything written before the field existed has none either. Both reach the provider once cloud attempts are merged. The read threw, and a throw inside a provider-level memo blanks the entire app rather than one row. Found by seeding a realistic attempt and reloading the production build | Fixed: the rule is now `attemptsForSkill` in `mastery.js`, where an attempt with no skill matches no skill — which is the correct answer, since a word game is not evidence for a C0 skill. The provider calls it instead of reaching into the field. A test executes the real function against a real `createAttempt` output, and removing the guard was confirmed to fail it | batch 2026-09-10 (Test Lab) |
| DEF-32 | High | QA-01 §13 | Every technical check told the parent to work inside the real app — start a lesson, answer five assessment questions — but `submitAttempt` binds the learner to the active profile, writes to `attemptsKey(learnerId)`, queues to the outbox and syncs to the cloud. Running a device check would therefore have written fabricated attempts against Jenn or Jess and fed them into mastery and review scheduling | Fixed: the technical checks run against a Test Lab harness that reuses the pure rules (`evaluateItem`, the durable-session comparison, idempotent delivery) with invented practice questions, the learner id `__testlab__`, and a `spelling-testlab-` storage namespace. No Test Lab check links to a learner route, which is enforced by test. `LearningProvider` and `submitAttempt` were not edited | batch 2026-09-10 (Test Lab) |
| DEF-33 | Medium | QA-01 §13 | The two-device check called `checkAvailability(check, {})` with an empty context, so it was permanently blocked rather than blocked-until-setup, and could never be run even once Firebase was configured | Fixed: a real preflight (`testLabPreflight.js`) requires a non-anonymous parent account, a reachable Firebase, and a Test Lab record that writes and reads back as its owner. There is deliberately no hand-set override; a passing preflight makes the check runnable, never done | batch 2026-09-10 (Test Lab) |
| DEF-34 | Medium | QA-01 §13 | The audio rows were a hand-written list that could drift from the assessment, they could only be heard by taking the assessment, and a contrast pair spoke one side only — so the question the check exists to answer, whether a pair is distinguishable, was unanswerable from the app | Fixed: all 44 rows are derived from the version-pinned assessment items and play in place with the assessment's own playback rule; the comparison side is built from the item's own distractor choice and labelled as a Test Lab comparison, never as assessment audio | batch 2026-09-10 (Test Lab) |
| DEF-35 | Low | QA-01 §13 | A parent's observation was recorded with `recordedBy` set to the active learner profile, attributing the parent's judgement to Jenn or Jess | Fixed: the log records the tester (default `Parent`), the device, and an observed learner only for a Family Pilot row where a named child really used the app. Entries written by the earlier version still load, still count, and are never rewritten | batch 2026-09-10 (Test Lab) |
| DEF-36 | Low | QA-01 §13 | The practice run's `Go offline` and `Send what is waiting` controls lived inside the question form, so they disappeared once the last question was answered — exactly when a queued offline answer is most likely still waiting, and the row asking the parent to confirm it arrives could not be completed. Going back online also did not flush, though the real app flushes on the browser's `online` event | Fixed: both controls sit outside the form and stay available after the run finishes, and going back online flushes the queue as production does. Found by driving the run to completion in the browser | batch 2026-09-10 (Test Lab) |
| DEF-37 | High | QA-01 §13 | The parent's first use of the Test Lab found it: Play only worked on the second tap, once the button read `Play again`. `ChecksPage` keyed `useCancellableSpeech` on the row being played, and the hook cancels whenever its scope key changes. The first tap set `playState` to that row, which changed the key, so React ran the cleanup for the previous key and `cancel()` aborted the utterance that had just started; the page then saw `reason: 'cancelled'` and suppressed its own report. The second tap worked because the key no longer changed. Every audio row in the listening, contrast and decoding checks was affected | Fixed: the hook is scoped to the page (`'checks'`). Starting another row still stops the previous one, because `play()` cancels first, and leaving the route unmounts the page. The other four call sites were audited and are correct — they key on learner or item, which change independently of pressing play. **The guard test had pinned the buggy line verbatim, so it locked the defect in rather than catching it**; it now asserts that the scope key must not depend on the row being played, and restoring the old key was confirmed to fail it. In the production build, one tap on the first, middle and last dictation row, both sides of a contrast pair and a decoding candidate each reported playback; headless Chromium has no voices, so the observable is the page's own playback report, which the defect suppressed | batch 2026-09-17 |
| DEF-38 | High | EDU-02 §7 / QA-01 §14 | Every renderer mapped `item.choices` in authored order, and the authored keys are patterned: the spelling pack cycles b, a, c, assessment Form A Part B runs b, a, b, a for all twelve prompts and Form B runs a, b, a, b. A learner who noticed the pattern could score without reading, and a retake showed identical positions, so repeating a form measured memory of a position rather than the skill | Fixed 2026-09-18: `src/learning/choiceOrder.js` derives a stable order from a seed held in the durable session, so a reload or a resumed tab keeps the order and only a new sitting reshuffles. Measured over 300 simulated sittings of the real packs and forms, the answer lands in each position within 5% of chance. Audio choices keep their authored order deliberately: the learner decides by listening, so position carries no answer information, and the Test Lab derives the parent's listening rows from those choices by index. Confirmed in the production build: five clean browser profiles produced five different orders, and a reload kept the order | batch 2026-09-18 (audit) |
| DEF-39 | High | EDU-01 §6 / DATA-01 §8 | A typed answer the key did not list evaluates to `pending`, but `LessonPage` passed only `evaluation.correct` to the flow. The learner read **Not yet**, was sent into a guided repair they did not need, was charged a retry towards the worked solution, and was shown the item's explanation, which argues for the key's single answer and reads as a correction. The engine called the answer undecided while the screen called it wrong, and nothing ever surfaced the queued text to a person | Fixed 2026-09-18: a pending result advances like a technical deferral, does not count a retry, reads `Sent for review`, and gets its own message instead of the explanation. Confirmed in the production build: typing `Our class won the challenge!` into the capitals lesson shows `Sent for review` and a `Continue` button. A parent-facing queue for reading these answers is still outstanding | batch 2026-09-18 (audit) |
| DEF-40 | Medium | EDU-01 §5 | Ten of every pack's twenty-four objects were unreachable. `lessonCatalog` served `independent` items `.slice(0, 6)`, so four never appeared, and nothing anywhere consumed the six `guided` items. A repeated lesson re-asked the same six questions in the same order | Fixed 2026-09-18: the practice window advances with the number of completions recorded for that learner and lesson, and the guided items are shown during teaching as worked examples, which is what `instruction_only` content is for. A test proves every authored item in every pack is reachable, counting the delayed-review items through the review queue's own stage rotation | batch 2026-09-18 (audit) |
| DEF-41 | High | §2 word data / MASTER_PLAN §0.4 | The Grade 6 word list was not a Grade 6 list. Its last eighty entries were the strictly alphabetical continuation of the Grade 4 list — Grade 4 stopped at `ocean`, these ran `offer` to `unusual` — so a Grade 6 session dictated `please`, `police`, `often` and `tomorrow`. Its middle block was spelling-bee vocabulary instead (`bureaucracy`, `connoisseur`, `hypocrisy`, `lieutenant`, `xylophone`). One list mixed Grade 3 words with words a Grade 9 student would struggle to spell, so a result from it meant nothing. The list also dictated the US `center`, `favorite` and `neighbor` while the lesson pack teaches the Canadian `colour` | Fixed 2026-09-18: each word declares the `level` it is practised at and the catalogue groups by level; twenty-nine spelling-bee words move to a `Challenge` group that is deliberately not labelled Alberta curriculum. A word's id is still derived from the category it is stored under, so re-levelling moves no progress row, and a corrected spelling pins the id it already had. Grade 6 now holds 91 words rather than 200: the list never contained two hundred real Grade 6 words, and padding it would recreate the defect, so the shortfall is left visible | batch 2026-09-18 (audit) |
| DEF-42 | High | QA-01 §14 | The suite verified that review records existed with matching ids, versions and dimension *names*; it never read an explanation, a key or a distractor. Its only content-quality assertion was `explanation.length >= 35`. The content validator accepted an item whose key named an option it did not offer, that repeated a choice id, that offered the same text twice or that marked two options correct. Every finding in the 2026-09-17 audit was invisible to the tests | Partly fixed 2026-09-18: the validator now rejects all five structural defects, comparing choice text with case intact because a capitals item's two options differ only in case by design. `readability.js` scores learner-facing prose, so the `age_accessibility` review dimension becomes a number rather than a label. `test/contentLint.test.js` runs the remaining five findings as real assertions against the real packs, forms and episodes, marked `todo` so the gap lives in code: the end-mark cue in 16 of 22 complete-sentence items, 82 two-option coin flips, four nonsense distractors, story prose at grade 8.7-14.1, and pack explanations above the grade they teach. Each needs a new item version and a correction record the parent resolves, and an open correction withholds its item, so installing them would withhold most of the content the pilot is about to use | batch 2026-09-18 (audit) |
| DEF-43 | High | EDU-01 §5 / QA-01 §14 | Shuffling the options (DEF-38) made twelve explanations false: they named the answer by where it sat — "The second choice can stand alone", "Choice b joins the two complete thoughts" — and after a shuffle that position is not the answer. Six were in the sentence pack and six in the assessment forms | Fixed 2026-09-18: each one now names the answer by its words, which is true whatever order the options appear in. The teaching, the key and the difficulty are unchanged. A lint rule asserts that no explanation points at an answer by its position, and restoring one was confirmed to fail it. A worked example is exempt: it compares two things quoted in its own prompt and offers no choices | batch 2026-09-18 (audit) |
| DEF-44 | Medium | EDU-01 §5 content governance | The correction mechanism had one severity. Any open correction withheld its item, which is right for a defect — content that teaches or grades something false — but wrong for a finding that only weakens measurement. Drafting the 2026-09-17 audit's corrections would have withheld 88 items: seven of ten independent questions in the sentence pack, four of six in punctuation, and every Part B prompt, leaving lessons that cannot serve six questions | Fixed 2026-09-18: a correction carries `severity`. `defect` withholds and is the default, so every record written before the field existed behaves exactly as it did. `improvement` leaves the item running while the drafted replacement waits, because nothing it teaches is false. An unknown severity is rejected rather than treated as harmless, and a test proves a defect still withholds | batch 2026-09-18 (audit) |
| DEF-45 | High | EDU-02 §7 / MASTER_PLAN §10 | The four listening contrasts per assessment form were EAL minimal pairs taken from the ESL benchmarks (ship/sheep, bit/beat, live/leave, pull/pool). The parent confirmed on 2026-09-17 that both children are native English speakers, so those eight prompts measure nothing about them | Drafted 2026-09-18 as corr.c0.011, awaiting the parent: homophones in a spoken sentence, the three sounds of the -ed ending, and the spoken possessive apostrophe. All eight are Part A and already blocked on the listening check, so no evidence changes hands and the parent's check will cover the new audio when it is recorded | batch 2026-09-18 (audit) |
| DEF-46 | Medium | EDU-01 §5 / QA-01 §14 | The drafted corrections raised every two-option question to three. Three options still leave a blind guess at one in three, and `deriveMastery` does not model chance, so a guesser reaches `developing` sooner than the content intends. The parent asked on 2026-09-18 for four options wherever the content supports it | Drafted 2026-09-18 in corr.c0.008 to corr.c0.010, awaiting the parent: every drafted question now offers four options except ten whose answer set is genuinely closed (three homophone spellings, the three sounds of -ed, three possessive forms of one word). Those ten are listed in `THREE_OPTION_ITEMS` with a per-item reason, and a test requires four options unless that list gives one, so "no fourth exists" cannot become "no fourth was attempted". Two classes of fourth option were rejected as defensible rather than wrong: a consistently past-tense sentence in a tense-consistency item, and a list without the final comma. A simulated install of every draft is validated by `validateContent`, so a fourth option that repeats an id or empties a choice is caught while the draft is still a proposal | batch 2026-09-18 (audit) |
| DEF-47 | Medium | EDU-01 §5 / MASTER_PLAN §10 | The first drafted story rewrite closed the readability finding by taking both episodes from grade 9.9 and 10.1 down to grade 5.2 and 5.3. The parent rejected it on 2026-09-18: the readers are in grade 5 and 6, so grade 5 prose leaves them nothing to stretch for. The rule being enforced was a ceiling, and a ceiling can only be satisfied by making prose easier | Fixed 2026-09-18 in the draft: the episodes now read at 7.6 and 7.5, roughly where a novel written for this age sits, with the fact box held below the prose at 8.4 and 8.7 because it carries the documented/invented disclosure and must be understood. The rule is now a band with a floor as well as a ceiling, applied to the episode's prose as a whole rather than to single short fields where a readability score is noise, and the floor was mutation-checked with a deliberately simplified intro | batch 2026-09-18 (audit) |
| DEF-48 | High | EDU-01 §5 content governance | `correctionInstalled` judged a correction installed only if EVERY item it names is at the replacement version, looking each one up in the content it was applied to. A correction spanning two packs — the option-count record covers the punctuation pack and the pronoun pack together — is applied to each pack separately, so each could see only half its items and treated the missing half as not installed. Installing the 2026-09-17 corrections therefore withheld 46 correct, fully corrected items and emptied three of the four lessons | Fixed 2026-09-18: the check judges only the items the content actually contains, and an unknown item id is still rejected outright by `validateCorrections`, so this excuses a cross-pack record without excusing a typo. Found by installing the corrections, not by reading the code: the suite went from green to 9 failures naming lessons that could no longer serve six questions | batch 2026-09-18 (install) |
| DEF-49 | Medium | EDU-02 §7 / QA-01 §14 | `testLabAudio.contrastRows` built two rows for every listening item: the audio the app plays, and one distractor spoken as a comparison, so the parent could answer "can you hear the difference between the two sides?". After corr.c0.011 that question is unanswerable by design — the replacements contrast `their` with `there`, and the three sounds of `-ed`, whose sides are supposed to sound identical — so a truthful answer would have been recorded as a defect in the audio | Fixed 2026-09-18: a row's shape is derived from the item. An item whose spoken text IS one of its options is a minimal pair and still gets both sides; an item that speaks a whole sentence gets one row asking whether the audio is clear and says the right sentence, naming the sentence it must say. Never a list of item ids, so a future listening item is classified by what it does. The contrast check is 8 rows rather than 16, and a fixture keeps the minimal-pair path covered now that the real forms hold none | batch 2026-09-18 (install) |
| DEF-50 | Low | EDU-01 §5 / QA-01 §14 | The retargeted listening replacements gave `c0.assessment.a.13` and `a.14` the same prompt text and the same three options, differing only in the audio and the key; `b.13` and `b.14` likewise. On screen they were the same question twice. The original prompts numbered each item ("Listen to contrast item A1") and the rewrite dropped the number | Fixed 2026-09-18 before install: the numbering is restored ("Listen to sentence A1"). Caught by the existing assertion that all 68 assessment prompts are distinct, which counted 66 | batch 2026-09-18 (install) |
| DEF-51 | Medium | EDU-01 §5 content governance | `correctionInstalled` judged a record by its `itemIds` alone. `corr.c0.013` names only `episodeIds`, so its scope was invisible: `validateCorrections` reported both episodes as unknown items and the replacement as never installed, and the record could not be resolved at all. The same record also carried `fromVersion: 1, toVersion: 2` when the episodes were already at version 2 | Fixed 2026-09-18: a correction's scope is its items and its episodes together, `validateCorrections` takes `episodes` alongside `items`, and the record now reads 2 to 3. An episode is not an item, but the rule is identical — the record is not installed until the thing it names is at the replacement version | batch 2026-09-18 (story install) |
| DEF-52 | Low | EDU-01 §5 / MASTER_PLAN §10 | The grade 7-8 story rewrite gave episode one a three-sentence recap. The recap is two sentences by design: it is read before the episode, not instead of it | Fixed 2026-09-18 before the install went out, by joining the last two sentences; the prose stayed in band at 7.7. Caught by the existing two-sentence assertion in `test/pilotFixtures.test.js`, which is the kind of rule a readability rewrite walks straight through | batch 2026-09-18 (story install) |
| OPEN-06 | High | MASTER_PLAN §10 / EDU-01 | The app measures 9 of the 214 Alberta Grade 5/6 ELAL Skills & Procedures outcomes, plus 1 partial, all of them in Conventions. 96 outcomes need a person (Oral Language and Writing in full); 108 are machine-scorable and not built, mostly Comprehension (39), Text Forms and Structures (30) and Vocabulary (26) | Open. Mapped in full from the parent-supplied source on 2026-09-18 in `src/data/curriculum.alberta.elal.json`, with one of four states and a reason on every outcome. Nothing is built against the gap yet; the parent decides what to build and in what order | open 2026-09-18 |
| OPEN-07 | High | MASTER_PLAN §10 | Two of the four C0 packs teach below Grade 5. Complete sentences is a Grade 3 outcome ("A sentence has two main parts, a subject and a predicate"); capitals and end marks is Grade 3 to 4. Subject/object pronouns and spelling patterns are at Grade 5 | Open, and recorded rather than silently corrected: below-grade content is still useful to a child who needs it, but these two lessons cannot be reported as a Grade 5/6 check. `appContentPlacement` names the statement each pack was judged against, and a test requires a quoted statement and a page number rather than an asserted grade | open 2026-09-18 |
| OPEN-08 | Medium | MASTER_PLAN §10 | Alberta ends Phonological Awareness after Grade 2 and Phonics and Fluency after Grade 4, so the 自然拼读 tile the parent asked for has no Grade 5/6 outcome behind it. ELAL has no pronunciation organizing idea at any grade, which leaves the five `PR.*` skills with no curriculum basis at all; they came from the EAL benchmarks and both children are native speakers | Open. The tile may still be built as Grade 1–4 foundation support if the children need it, but it must never be labelled Alberta Grade 5/6. Recorded in `organizingIdeasNotInGrade56` and `skillsWithNoGrade56Outcome`, both held by test | open 2026-09-18 |
| DEF-53 | Medium | QA-01 §14 | The first extraction of the curriculum read each PDF page as flat text. Grade 5 and Grade 6 share a page in two columns, so the two grades interleaved and statements would have been attributed to the wrong grade — the exact failure that would have made the whole mapping quietly false | Fixed 2026-09-18 before any mapping was written: the extraction keeps each text item's x position and assigns it to a column, and a second pass uses the left margin to split statements, because a bullet's wrapped continuation is indistinguishable from a new statement without it. A test asserts the two grades carry a different guiding question and learning outcome on every organizing idea, which no column mix-up survives | batch 2026-09-18 (curriculum) |
| OPEN-09 | Medium | EDU-01 §5 / MASTER_PLAN §10 | The C1 comprehension packs carry their passages inline in the prompt, two to four sentences each, so they run on the existing lesson renderer with no UI change. That measures inference and stated-versus-inferred honestly, but it is not the same as reading a grade-level passage of 300 words and answering several questions about it, which is what Alberta's Comprehension outcomes describe and what a PAT-style reading section would ask | Open. The short-passage items are worth having on their own; the longer reading section is a separate piece of work needing a passage renderer and a multi-question item group. Recorded so the short form is never mistaken for the whole outcome | open 2026-09-18 |
| DEF-54 | High | MASTER_PLAN §Season 1 / R3-G1 | Six new lesson packs were built on 2026-09-18 with no story episode for any of them, against the parent's standing decision on 2026-09-17 to keep the story for everything. Nothing counted episodes against the plan's total, so the gap was invisible: the app had 2 of the 12 episodes MASTER_PLAN commits to and nothing said so. The same blind spot had already hidden that `GR.subject-object-pronouns` has been pilot-approved since 2026-09-09 with no episode at all | Fixed 2026-09-18: five episodes written, taking the story to 7 of 12, each placed in the chapter the plan assigns it by language focus rather than by when it was written. `src/data/story.ledger.json` is generated from the story files by `tools/build_story_ledger.mjs` and counts written against planned per chapter, so an episode cannot be written or lost without the count moving, and a chapter short of its two must record what blocks it | batch 2026-09-18 (story) |
| DEF-55 | Medium | EDU-01 §5 / MASTER_PLAN §10 | All five new episodes were drafted with fact boxes reading at grade 10.8 to 13.3 against a ceiling of 9 — the same fault corr.c0.013 had just corrected in the original episodes, reproduced in new content within the hour. The prose was in band at 6.7 to 7.5; the fact box is where long documented claims accumulate | Fixed 2026-09-18 before the episodes were committed: each fact box rewritten in shorter sentences, landing at 7.0 to 9.0. Caught by the readability band being applied to new episodes from their first test rather than only to the ones a correction had already touched | batch 2026-09-18 (story) |
| OPEN-05 | Medium | EDU-01 §5 | 44 spelling and dictation questions offer three options rather than four. They were never part of the 2026-09-17 two-option finding — they have always had three — so no correction record covers them, and `corr.c0.008` to `corr.c0.010` did not touch them | Not fixed, deliberately. Raising them means authoring a fourth distractor for each, which is content the parent has not reviewed, and unreviewed content must not reach a child. Drafted 2026-09-18 as corr.c0.014 (21 items at version 1) and corr.c0.015 (9 already at version 2), awaiting the parent: 30 of the 44 take an honest fourth option, and the other 14 keep three with a per-item reason in `THREE_OPTION_ITEMS` — eight closed answer sets, two closed error sets, and four audio items whose fourth option would be a fourth recording of an invented word, which is new spoken content needing the very listening check those prompts are already blocked on. Two classes of fourth were written and refused as defensible rather than wrong: `color` for `colour`, and "On Monday we begin." without the optional comma | open 2026-09-18 |

## Decisions and change log

| changeId | requirementId | proposedChange | reason | affected | decisionOwner | decision | decidedAt |
|---|---|---|---|---|---|---|---|
| CHG-01 | EDU-01 | Set `allowReview: true` on the six typed C0 punctuation items (pack version unchanged) | Master plan §8 requires a `Review this answer` path for reasonable unlisted answers; answer keys and explanations are untouched, so existing challenge/educational review records remain valid | `packs.c0.draft.js`, evaluator tests | Claude (routine implementation; content correction without policy change) | Applied; Codex to confirm at audit | 2026-09-08 |
| CHG-02 | DATA-01 | Mastery derivation version 1 → 2 | Regression rule now applies below `secure`; derived status may change for the same attempts, so the version is bumped and no attempt data is rewritten | `mastery.js` | Claude | Applied | 2026-09-08 |
| CHG-03 | DATA-01 | Keep `src/learning/sessionEngine.js` although no page imports it | It is unit-tested and models the §9 session entity (revision guards, frozen responses); the shipped lesson/assessment pages use `lessonFlow.js` plus `useDurableSession`. Deleting it would remove a candidate for the R3 session model; wiring it in is a larger change than this increment | none | Claude (recorded for Codex) | Deferred; revisit at R3 | 2026-09-08 |
| CHG-04 | DATA-04 / QA-01 | Firestore rules remain verified by text assertions only | No Firestore emulator is available in the implementation environment; `test/firestoreRules.test.js` checks rule text, not behaviour | `firestore.rules` | Claude (recorded for parent + Codex) | Open follow-up: run the emulator rules suite when Firebase setup is authorized | 2026-09-08 |
| CHG-06 | GOV-01 / EDU-01 | Add an `approved-for-private-pilot` lifecycle state between integration and learner testing | The plan's release rules were circular: content could not enter delayed review until released, release required learner testing, and the pilot was that learner test. Pilot-approved content runs the full loop but its answers form a separate pilot evidence record that is never presented as validated progress | `pilotApproval.js`, `mastery.js`, `reviewScheduler.js`, `reviewQueue.js`, `contentValidator.js`, `contentManifest.js`, providers and screens | Parent (recorded 2026-09-08) | Accepted as a controlled change to the master plan. Approvals were recorded on 2026-09-09 (see CHG-11) | 2026-09-08 |
| CHG-07 | SPEECH-01 / EDU-02 | Withdraw the requirement for 24 human recordings and 16 external specialist checks; accept reviewed, truthfully labelled model audio plus a parent listening check and a Codex educational review | The master plan (§10) asks for suitable model audio where available, truthful labelling otherwise, and human review specifically for isolated phoneme audio. The stricter rule was an implementation choice presented as an obligation, and no external specialist or native-speaker rater is available to this family | `contentValidator.js`, `audio.c0.js`, `AUDIO_REVIEW_HANDOFF.md`, `r2GateTracker.js` | Parent (recorded 2026-09-08) | Accepted. A human recording is still required for isolated phoneme audio, and model speech may never be labelled as a human recording | 2026-09-08 |
| CHG-08 | EDU-02 | Redesign the invented-word decoding prompts as print-only segmentation plus an unscored read-aloud self-comparison | The prompts supplied the pronunciation, so they did not measure decoding. With no rater available, a recorded reading cannot produce a score, so it is kept as self-comparison and the report states that independent decoding is not measured rather than implying coverage | `corrections.c0.json`, assessment report copy | Parent (recorded 2026-09-08) | Accepted; the replacement items await Codex review before use | 2026-09-08 |
| CHG-09 | DATA-02 | Word totals become a cache derived from the immutable attempt record instead of increment writes | Increment writes are not idempotent under retry, and writing device-local totals could lower another device's count. Deriving from attempts makes any device able to recompute the same value | `progressAggregate.js`, `outboxSync.js`, `WordProvider.jsx`, `LearningProvider.jsx` | Parent (recorded 2026-09-08) | Accepted | 2026-09-08 |
| CHG-05 | SPEECH-01 | Recording copy now says recordings stay on this device until deleted | Actual behaviour is durable IndexedDB storage; the earlier copy implied session-only storage. A delete control was added rather than changing storage semantics | `RecordingAnswer.jsx`, `indexedDb.js` | Claude | Applied | 2026-09-08 |
| CHG-10 | EDU-02 | Replace the invented-word decoding prompts with a machine-scored receptive audio choice | Claude proposed a print-only segmentation item plus an unscored read-aloud, which would have left the decoding track with no scored result. The parent chose a task the learner can be scored on: read the printed word, pick the recording that matches. Its result is reported as recognizing a plausible pronunciation from spelling, and the track states that reading aloud unaided is still not measured | `assessment.c0.draft.js`, `contentValidator.js`, `assessmentReport.js`, `AssessmentRunner.jsx` | Parent | Applied 2026-09-09; the twelve recordings need the listening check before pilot use | 2026-09-09 |
| CHG-11 | GOV-01 | Approve the four C0 packs, both chapter-one episodes, and the 28 Part B assessment prompts for a private family pilot | The six audit corrections are resolved and their replacements installed, so the reviewed content can be exercised end to end. Pilot answers form a separate evidence record and are never presented as validated mastery | `pilotApproval.c0.json` and every consumer of the lifecycle | Parent | Applied 2026-09-09. All 20 Part A prompts per form are excluded: they depend on audio nobody has listened to, or on a rater we do not have | 2026-09-09 |
| CHG-12 | EDU-03 / QA-01 | Read-aloud recordings are practice, never evidence | The family are not native speakers and no qualified rater is available, so no path may depend on a human rating to produce evidence. The recording stays on the device, is never scored, and produces no attempt record | `AssessmentRunner.jsx`, `corrections.c0.json` | Parent | Applied 2026-09-09 | 2026-09-09 |
| CHG-13 | QA-01 | Test the real functions headlessly rather than adding a browser-rendering harness | The suite already runs without the app; the gap was rules trapped in components and tests that only matched source text. Extracting the rules and hand-writing two narrow fakes closes that gap with no new dependency. Rendering components headlessly would need jsdom, a JSX loader, and a testing library, which is a large dependency footprint for a project with two devDependencies | `src/learning/*`, `src/persistence/*`, `test/fakes/*` | Parent | Applied 2026-09-09. Component rendering deliberately not adopted | 2026-09-09 |
| CHG-14 | QA-01 | Keep some source-text tests, renamed as guards | A few protect truthful learner-facing wording, such as the recording and audio copy, which no behavioural test would catch. They are kept and relabelled so nobody reads them as behaviour coverage | `test/*.test.js` | Parent | Applied 2026-09-09 | 2026-09-09 |
| CHG-15 | QA-01 / GOV-01 | Put the human checks inside the app, at `/checks`, instead of only in a handoff document | The remaining gates all depend on someone opening the app and observing something a test cannot observe. A document beside the app is read once and lost; a page inside it can be opened on the iPad being tested, walked through one row at a time, and paused. Sixty-one rows across eight checks now carry their own steps, pass criteria and stated limit | `humanChecks.js` (data and rules), `checkLog.js`, `ChecksPage.jsx`, `/checks`, Home, `ParentPage.jsx` | Parent | Applied 2026-09-09. Recording a result is a parent observation on one device: it is not mastery evidence, it releases nothing, and `gateStateAfterChecks` returns the gate's own state however many rows are ticked, which is enforced by test | 2026-09-09 |
| CHG-16 | QA-01 / GOV-01 | Split `/checks` into an isolated Technical Test Lab and a Family Pilot Observation area | The first version made no distinction, so a check meant to test the machine sent the parent into the real app and wrote real attempts against a child. The two kinds of check need opposite properties: a technical check must touch nothing, and a learner-observation check must use the real thing and say so. The Test Lab reuses the pure rules against an isolated namespace rather than a second implementation of them; the Pilot area names the child and requires an explicit acknowledgement before it will open a route | `humanChecks.js` (data and rules), `testLabAudio.js`, `testLabRun.js`, `testLabPreflight.js`, `testLabStore.js`, `checkLog.js`, `ChecksPage.jsx`, `firestore.rules` | Parent | Applied 2026-09-10 | 2026-09-10 |

## Estimate (master plan §0.1)

Hands-on effort is Claude implementation time; waiting time is external and not under implementation control. No calendar date is promised for any release; a proposed date can only be set once the named external inputs exist.

| Release | Hands-on remaining | Waiting on | Proposed date |
|---|---|---|---|
| R1 Repair | 0–1 day: assemble the R1-G5 candidate report once device evidence exists | Real-iPad regression check (R2-G6 style) for the repaired games; parent publication decision | Not set: blocked on iPad evidence and parent decision |
| R2 Pilot | 2–4 days after inputs arrive: complete the listening check (24 dictation and contrast prompts plus 12 receptive decoding recordings), complete the educational review of the 16 decoding and speaking prompts, promote Part-A items, run the two-device checks, fix defects found on the iPad and in learner testing | Parent listening check + educational review (R2-G1); Firebase Email/Password + rules deployment (R2-G4); real iPad (R2-G6); learner testing; family pilot window (R2-G7, ≥2 weeks observation when practical) | Not set: R2-G1/G4/G6 inputs are all external |
| R3 Full | 15–25 days hands-on: import/validate 38 packs (912 objects) and 10 episodes, map packs to episodes/workshops, migration and rollback tests, acceptance matrix, docs | C1/C2 authoring by GPT after R2 pilot feedback; per-chapter historical sources (chapter 3 requires Indigenous nation/community sources) | Not set: depends on R2 pilot outcome |

## Acceptance-scenario trace (master plan §13)

Evidence types: unit = automated test in `test/`; browser = production build in headless/desktop Chromium; iPad = real device; pilot = children used it. Only the highest evidence actually recorded is listed.

| # | Scenario | Evidence | Where |
|---|---|---|---|
| 1 | Duplicate useRef removed → build passes | unit/CI | `npm run build`; GitHub Actions on PR #17 |
| 2 | All correct except skipped last word → no perfect badge | unit + browser | `test/r1Core.test.js` (perfect score); browser skip check |
| 3 | Try Again after completion → fresh session | unit + browser | `test/r1Core.test.js`; browser restart check |
| 4 | Yesterday's successes do not satisfy today's five words | unit + browser | `dailyChallengeComplete` tests; browser daily route |
| 5 | Five crossword answers → five distinct durable events | unit + browser | `test/r1Core.test.js` batched attempts; crossword check |
| 6 | Word longer than grid → valid larger puzzle | unit + browser | `test/r1Core.test.js` generator; 11×11 browser puzzle |
| 7 | Flashcard Got It / hint win → practice only | unit | `self_report` and eligibility tests |
| 8 | Wrong, helped repair, correct transfer → three states, first answer kept | unit + browser | `test/lessonFlow.test.js`, `test/learningEngine.test.js`; lesson browser repair |
| 9 | Double-tap Submit / reconnect retry → one attempt, one award | unit | `transitionSession` idempotency; `test/outbox.test.js` exactly-once delivery; `test/achievements.test.js` merge; SpeedRound guard (source); `test/progressAggregate.test.js` proves a repeated flush writes the same totals |
| 10 | Switch profile during speech/request/save → no leakage | browser | learner-scoped speech/session checks (EDU-03 row) |
| 11 | Close tab midway; reopen → same state, first attempts, recap | unit + browser | durable session tests; lesson reload shows resume recap and question 2 |
| 12 | Offline, answer five items, reconnect → all reconcile once | headless end to end | `test/attemptSync.test.js` runs the whole sequence against an in-memory account: five answers queued offline produce five documents, and a second reconnect writes nothing new. It also proves a sync sends only the learner it is syncing. Totals are derived rather than pushed and written inside a transaction (`test/progressAggregate.test.js`). The reconnect listener itself is still only source-verified, and no live Firebase run has happened |
| 13 | Two linked devices, separate learner sessions | none (contract tests only) | `test/sessionSync.test.js`; a device that cannot see every attempt is now held back from lowering a shared total (`test/progressAggregate.test.js`); still requires Firebase setup |
| 14 | Two devices open same session → takeover/read-only, stale write rejected | headless end to end + browser (same browser, two tabs) | `test/persistenceHeadless.test.js` drives the real `claimRemoteSession` and `saveRemoteSession` against an in-memory account: first claim, read-only second device, takeover with an epoch bump, and refusal of a save built on a superseded revision. Pure decisions in `test/sessionSync.test.js`, `test/sessionLease.test.js`; two-tab browser check. A fake is not two devices, so cross-device remains unverified |
| 15 | Legacy import repeated twice → no duplicates, history intact | headless end to end | `test/importPreview.test.js` for the preview counts; `test/attemptSync.test.js` proves an account that already holds history is not merged into until the parent decides, and pushes once they do; `test/progressAggregate.test.js` proves an imported total does not count the answers it already contains a second time. Live account path unverified |
| 16 | Edmonton midnight/DST transition → one day key | unit | `test/r1Core.test.js` spring-forward, fall-back, foreign TZ |
| 17 | Alternative valid sentence repair → accepted or pending | unit | `test/learningEngine.test.js` structural repair and pending review, including the rejection of a lower-cased pronoun I |
| 18 | Stripping commas must fail | unit | `test/learningEngine.test.js` punctuation test |
| 19 | Mic denied/silence/unsupported → retry/defer, no wrong mark | unit + browser | `test/recording.test.js`; assessment technical-failure browser check; lesson deferral unit test. Real mic not exercised |
| 20 | Reassessment with exposed form → exposure disclosed | unit + browser | `test/assessmentReport.test.js`; `/assessment` comparison view |
| 21 | Correct practiced examples, fail unseen transfer → developing | unit | `deriveMastery` secure criteria (unseen ≥ 3, transfer required) |
| 22 | Finish chapter with guided corrections → story advances, mastery evidence-based | unit + browser | `test/storyProgress.test.js`; story unlock browser check |
| 23 | History source missing / fictional quote unlabelled → validation fails | unit | `validateContent` episode source and fiction-label rules |
| 24 | Reviewed content with a known defect → withheld, coverage reported | unit + browser | `test/contentCorrections.test.js`; Form A serves 31 of 34 prompts with the withholding stated on screen |

Scenario 13 still has no evidence of any kind. Scenarios 12, 14, and 15 now run headlessly against in-memory stores, which is real execution of the real code but is not a live Firebase run and not a second device. Scenarios 10 and 19 lack real-device evidence. Scenario 24 was added by the 2026-09-08 audit response.

## Known limitations and external gates

- No real-iPad Safari or installed-home-screen test has occurred.
- Browser recording controls compile and use user-triggered permission, durable blobs, and playback; microphone permission/playback was not exercised because that requires an explicit device permission decision and real-iPad follow-up.
- Firebase anonymous authentication is not enabled for the current project, so only local-only behavior was exercised in the browser.
- Parent sign-in UI and additive sync are implemented, but Firebase Email/Password is not enabled and no real account or second-device test has been performed.
- Same-browser resumable-session ownership and takeover are browser-verified; true two-device ownership still requires authenticated Firebase arbitration and testing.
- The cloud-session transaction contract is wired for non-anonymous parent accounts, but its Firestore rules are not deployed and the authenticated/two-device path is unverified.
- Shared family identity, Part-A assessment/audio review, full assessment release, additional sourced story chapters, pronunciation recording, and family pilot remain R2/R3 work.
- IndexedDB session snapshots and the attempt outbox are integrated locally, and direct production-browser recovery is verified for missing and malformed localStorage mirrors; remote reconciliation, C0 learner testing, Part-A integration, pronunciation capture, and the family pilot remain incomplete.
- The implementation branch is published in draft GitHub PR #17; each increment must pass GitHub Actions before merge. 2026-09-08 increments (Claude, batches 1–7) fix engine, persistence, import, lesson, assessment, game, and documentation gaps only; no external-evidence cell in this ledger was upgraded.
- Six C0 content corrections are open. Their items are withheld, so the assessment forms and one lesson currently have reduced coverage, and no content can be pilot-approved or released until Codex resolves them.
- Independent decoding is not measured by the current or proposed assessment: no qualified rater is available, so read-aloud items are kept as unscored self-comparison and the report must say so.
- Word totals are derived from immutable attempts, but the derivation has never run against a live Firebase project; a device that cannot see every attempt holds its write back rather than lowering a shared count.
- Production publication and Firebase-console changes remain parent-authorized external actions.
